# Bowlna — Netlify configuration
# No build step needed: this is a static site. Just publish the folder as-is.

[build]
  publish = "."
  command = ""

# Long-cache the image + runtime assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
